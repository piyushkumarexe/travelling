org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=1g -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
# NOTE: `android.enableR8=true` was removed here — AGP 8 deleted that flag and
# prints "The option 'android.enableR8' is deprecated" on every build. R8
# minification is now controlled by the release build type (isMinifyEnabled).
